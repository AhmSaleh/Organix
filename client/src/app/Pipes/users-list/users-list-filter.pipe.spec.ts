import { UsersListFilterPipe } from './users-list-filter.pipe';

describe('UsersListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UsersListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
