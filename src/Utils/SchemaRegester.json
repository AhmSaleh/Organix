{
  "type": "object",
  "properties": {
    "email": {
      "type": "string",
      "format": "email"
    },
    "password": {
      "type": "string",
      "minLength": 8,
      "maxLength": 20,
      "format": "password"
    },
    "name": {
      "type": "string",
      "minLength": 2,
      "maxLength": 20
    }
  },
  "required": ["email", "password", "name"]
}
